## What / why

<!-- Summarize the change and the motivation behind it. -->

Closes #<!-- issue number, if any -->

## Checklist

- [ ] Formatted (`gofmt -w .`, or the `.githooks/pre-commit` hook is installed)
- [ ] `go vet ./...` passes
- [ ] `go build ./...` passes
- [ ] `go test ./...` passes
- [ ] Docs updated (`README.md`, `docs/`) if public behavior changed
